# 🔒 راهنمای بستن تمام Pull Request ها

## 📝 توضیحات

این اسکریپت برای بستن تمام Pull Request های باز در ریپوزیتوری طراحی شده است.

## ✅ پیش‌نیازها

1. **Python 3.x** نصب شده باشد
2. **GitHub Personal Access Token** با دسترسی‌های زیر:
   - `repo` (Full control of private repositories)
   - یا حداقل: `public_repo` برای ریپوزیتوری عمومی

## 🔑 ایجاد GitHub Token

1. به [GitHub Settings > Developer settings > Personal access tokens](https://github.com/settings/tokens) بروید
2. روی **Generate new token (classic)** کلیک کنید
3. یک نام معنادار انتخاب کنید (مثلاً: "Close PRs Script")
4. دسترسی **repo** را انتخاب کنید
5. روی **Generate token** کلیک کنید
6. توکن را کپی کنید (فقط یک بار نمایش داده می‌شود!)

## 🚀 استفاده

### گام ۱: نصب وابستگی‌ها

```bash
cd Auto
pip install -r requirements.txt
```

### گام ۲: تنظیم توکن

#### روش اول: متغیر محیطی موقت (Linux/Mac)

```bash
export GITHUB_TOKEN='your_token_here'
python scripts/close_all_prs.py
```

#### روش دوم: متغیر محیطی موقت (Windows)

```cmd
set GITHUB_TOKEN=your_token_here
python scripts/close_all_prs.py
```

#### روش سوم: در یک خط

```bash
GITHUB_TOKEN='your_token_here' python scripts/close_all_prs.py
```

### گام ۳: اجرای اسکریپت

اسکریپت به صورت خودکار:
1. ✅ لیست تمام PR های باز را می‌گیرد
2. ✅ تعداد آن‌ها را نمایش می‌دهد
3. ⚠️  از شما تأیید می‌خواهد
4. ✅ تمام PR ها را می‌بندد
5. ✅ گزارش نهایی را نمایش می‌دهد

## 📋 مثال خروجی

```
🔍 Fetching GitHub token...
📋 Fetching open pull requests for ramincsy/Auto...

📊 Found 30 open pull request(s):
  - PR #3431: [WIP] Close all open pull requests
  - PR #3430: Daily Update daily-contribution-2026-01-28
  - PR #3429: Daily Update daily-contribution-2026-01-27
  ...

⚠️  WARNING: This will close ALL open pull requests!
Are you sure you want to continue? (yes/no): yes

🔄 Closing pull requests...
  Closing PR #3431... ✅
  Closing PR #3430... ✅
  Closing PR #3429... ✅
  ...

📊 Summary:
  ✅ Closed: 30
  ❌ Failed: 0
  📋 Total: 30

✨ All pull requests closed successfully!
```

## ⚠️ هشدارها

- این عملیات **غیرقابل بازگشت** نیست (می‌توانید PR ها را دوباره باز کنید)
- اما بهتر است قبل از اجرا مطمئن شوید
- اسکریپت از شما تأیید می‌خواهد قبل از بستن PR ها

## 🔐 امنیت

- **هرگز** توکن خود را commit نکنید
- توکن را در فایل `.env` یا متغیر محیطی قرار دهید
- پس از استفاده، توکن را بی‌اعتبار کنید اگر نیازی ندارید

## 💡 نکات

- اگر تنها می‌خواهید تعداد PR ها را ببینید، می‌توانید در مرحله تأیید "no" بزنید
- اسکریپت تمام صفحات PR ها را pagination می‌کند
- اگر خطایی رخ دهد، پیام خطا را نمایش می‌دهد

## 🐛 عیب‌یابی

### خطا: "No GitHub token found"
- مطمئن شوید `GITHUB_TOKEN` یا `GH_TOKEN2` تنظیم شده است
- توکن را چک کنید که معتبر باشد

### خطا: "Error fetching PRs: 401"
- توکن شما نامعتبر است یا منقضی شده
- یک توکن جدید ایجاد کنید

### خطا: "Error fetching PRs: 403"
- توکن شما دسترسی کافی ندارد
- مطمئن شوید scope `repo` فعال است

### خطا: "Error fetching PRs: 404"
- نام ریپوزیتوری اشتباه است
- یا دسترسی به ریپوزیتوری ندارید

---

**ساخته شده برای**: ramincsy/Auto
**زبان**: Python 3.x
**وابستگی‌ها**: requests
